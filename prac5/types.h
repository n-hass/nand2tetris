#ifndef UINTTYPES_H
#define UINTTYPES_H

#include <cstdint>
#include <map>
#include <string>
typedef uint16_t u16;
typedef uint32_t u32;

#define SPAIR(x,y) pair<std::string, u16>((x),(y)) // a symbol pair

#endif